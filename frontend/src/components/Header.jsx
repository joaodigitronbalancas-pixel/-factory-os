function Header() {
  return (
    <div
      style={{
        background: "#fff",
        padding: "15px",
        borderRadius: "10px",
        marginBottom: "20px",
        display: "flex",
        justifyContent: "space-between"
      }}
    >
      <h2>Factory OS 4.0</h2>

      <div>
        João Leopoldino | Administrador
      </div>
    </div>
  );
}

export default Header;